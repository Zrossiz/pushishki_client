import { Layout } from "@/layout/Layout"

const OrderPage = () => {
    return (
        <Layout title="Оформление | Пушишки">
            <div>order</div>
        </Layout>
    )
};

export default OrderPage;