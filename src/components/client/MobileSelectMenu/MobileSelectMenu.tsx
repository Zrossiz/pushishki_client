import { MobileSelectMenuProps } from './MobileSelectMenu.props';
import styles from './MobileSelectMenu.module.scss';

export const MobileSelectMenu = ({
  items,
  selectedItems,
  onChange,
  selectMenuName,
}: MobileSelectMenuProps) => {
  return <div>mobile select menu props</div>;
};
