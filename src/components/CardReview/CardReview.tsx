import styles from './CardReview.module.scss';
import { CardReviewProps } from './CardReview.props';

export const CardReview = ({}: CardReviewProps) => {
    return (
        <div>review</div>
    )
}