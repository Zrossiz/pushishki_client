import { ReviewListItemProps } from "./ReviewListItem.props";
import styles from './ReviewListItem.module.scss';

export const ReviewListItem = ({  }: ReviewListItemProps) => {
    return (
        <div></div>
    );
};