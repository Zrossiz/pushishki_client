import styles from './CountryForm.module.scss';
import { CountryFormProps } from './CountryForm.props';

export const CountryForm = ({ setOpen, isEdit }: CountryFormProps) => {
    return (
        <div></div>
    )
}