import { ProductVariantFormProps } from "./ProductVariant.props";
import styles from './ProductVariantForm.module.scss';

export const ProductVariantForm = ({ id }: ProductVariantFormProps) => {
    return (
        <div></div>
    );
};