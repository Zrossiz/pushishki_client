import styles from './CategoryListItem.module.scss';
import { CategoryListItemProps } from './CategoryListItem.props';

export const CategoryListItem = ({}: CategoryListItemProps) => {
    return (
        <div></div>
    )
}