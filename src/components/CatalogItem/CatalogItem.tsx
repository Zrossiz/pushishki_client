import { CatalogItemProps } from "./CatalogItem.props"

export const CatalogItem = ({ 
    image,
    price,
    name,
    availibility
}: CatalogItemProps) => {
    return (
        <div></div>
    )
}