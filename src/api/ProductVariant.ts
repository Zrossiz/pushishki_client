export const getProductVariants = async () => {
    try {
        
    } catch (err) {
        console.log(err);
        return {
            message: 'Ошибка при получении вариантов товара'
        } 
    }
}