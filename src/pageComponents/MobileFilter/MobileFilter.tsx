import styles from './MobileFilter.module.scss';
import { MobileFilterProps } from './MobileFilter.props';

export const MobileFilter = ({}: MobileFilterProps) => {
    return (
        <div>Фильтр</div>
    );
};